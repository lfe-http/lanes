[{kernel, [
    {logger, [
        {handler, default, logger_std_h,
         #{formatter => {logjam, #{
            colored => true,
            map_depth => 3,
            term_depth => 50,
            time_designator => $\s,
            time_offset => "",
            time_unit => second,
            strip_tz => true,
            level_capitalize => true
          }}}
        }
    ]},
    {logger_level, debug}
 ]}
].
